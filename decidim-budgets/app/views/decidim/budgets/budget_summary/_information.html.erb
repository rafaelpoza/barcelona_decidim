<% title = current_workflow.single? ? translated_attribute(budget.title) : t("#{"checked_out." if current_order_checked_out? }title", scope: i18n_scope) %>
<% title = current_workflow.single? ? translated_attribute(budget.title) : t("#{"checked_out." if current_order_checked_out? }title", scope: i18n_scope) %>
<% subtitle = translated_attribute(budget.title) unless current_workflow.single? %>

<% description = current_order_checked_out? ?
  t("checked_out.description", scope: i18n_scope, cancel_link: link_to(t("cancel_order", scope: i18n_scope), budget_order_path(return_to: "budget"), method: :delete, class: "cancel-order", data: { confirm: t("are_you_sure", scope: i18n_scope) })) :
  current_rule_description %>

<h3 class="heading3">
  <%= title %>
  <%= content_tag :small, subtitle if subtitle.present? %>
</h3>

<p>
  <%= raw description %>
</p>
